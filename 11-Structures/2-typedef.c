#include<stdio.h>
typedef float Decimal;
typedef int Integer;
int main(){
    float y;
    // I want to change float datatype's name.
    Decimal x; // Decimal is doing the same thing as float.
    Integer z = 5;
    return 0;
}